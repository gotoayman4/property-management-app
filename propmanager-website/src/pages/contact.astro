---
/**
 * @file Arabic contact page — "/contact".
 */
import ContactPage from '../components/pages/ContactPage.astro'
import { t } from '../i18n'
import BaseLayout from '../layouts/BaseLayout.astro'

const locale = 'ar'
const s = t(locale)
---

<BaseLayout locale={locale} title={s.contactPage.title} description={s.contactPage.sub}>
  <ContactPage locale={locale} />
</BaseLayout>
