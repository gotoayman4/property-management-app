---
/**
 * @file English terms of use — "/en/terms".
 */
import TermsPage from '../../components/pages/TermsPage.astro'
import { t } from '../../i18n'
import BaseLayout from '../../layouts/BaseLayout.astro'

const locale = 'en'
const s = t(locale)
---

<BaseLayout locale={locale} title={s.legal.termsTitle} description={s.meta.siteDescription}>
  <TermsPage locale={locale} />
</BaseLayout>
